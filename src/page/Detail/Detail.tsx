import React from 'react';
import {SafeAreaView} from 'react-native';
import {ScreenProps} from '@/types';

const Detail: React.FC<ScreenProps<'Detail'>> = () => {
    return <SafeAreaView />;
};

export default Detail;
